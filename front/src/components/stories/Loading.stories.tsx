import React from 'react';
import Loading from '../loading/Loading';  // Ajusta la ruta según la ubicación de tu archivo

export default {
  title: 'Components/Loading',  // Nombre de la categoría y el componente
  component: Loading,
};

export const Default = () => <Loading />;